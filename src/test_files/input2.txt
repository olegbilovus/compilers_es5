var s, s1: string;\
var a ^= 3;
c, b: boolean;\


proc main(v: string, n: integer):
  var b: real; s, i : string;\
  s ^= "hello" + "world";
  b, s ^= f(4+5, a>=b);
  p(@b, s);

  --> "Input is " $(i+1) "\nThanks";
  -->! "Hello" + "World" + "!!";
  <-- "Enter input:" + "\n" $(i) "Done";

  while true do
    var aaa: real;\
    b ^= a + 4;
  endwhile;
endproc

proc p(out a: integer, b: boolean):
  var c, e ^= 4, 5;
  r: boolean;\

  if b then
    a ^= (4 + 5) * 7;
    c ^= a > 5;
  elseif !r then
    sxy ^= 4;
    a ^= 7 * -4;
    var sxy: real;\
  else
    var a: boolean;\
    a ^= true;
  endif;

  while a <= 10 do
    r, a ^= f();
    a ^= a + 1;
    var v: integer;\
    v ^= 4 + a;
    while true do
      b ^= 4;
      var a: real;\
    endwhile;
  endwhile;
endproc

var d, e ^= 3.4, "hello";\

func f(a: integer, b: string) -> real, boolean:
  c ^= b;
  return a, true;
endfunc

func bol() -> boolean:
  var boool: string;\
  return true;
endfunc
